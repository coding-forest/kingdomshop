const { MessageEmbed } = require('discord.js')

module.exports =  () => {
    return new MessageEmbed()
    .setTitle('🎫 جني : ')
    .setColor('#4BB543')
    .setDescription('إضغط على الزر لتجني التذكرة')
}
